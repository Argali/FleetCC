# Copy this to .env and fill in the values
JWT_SECRET=change_this_to_a_long_random_string
GPS_PROVIDER=mock
PORT=3001
# URL of the deployed frontend (for CORS). Example:
# FRONTEND_URL=https://argali.github.io
FRONTEND_URL=http://localhost:5173
